# Copy this file to .env and fill in your own values. .env is gitignored.

# Required for scripts/run_pipeline.py's GenAI advisory stage.
ANTHROPIC_API_KEY=

# Optional: only set this if routing requests through a self-hosted gateway
# instead of api.anthropic.com.
# ANTHROPIC_BASE_URL=
